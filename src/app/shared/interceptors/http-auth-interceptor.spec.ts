import { HttpAuthInterceptor } from './http-auth-interceptor';

describe('HttpAuthInterceptor', () => {
  it('should create an instance', () => {
    expect(new HttpAuthInterceptor()).toBeTruthy();
  });
});
